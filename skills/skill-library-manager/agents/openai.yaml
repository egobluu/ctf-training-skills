interface:
  display_name: "Skill Library Manager"
  short_description: "Audit skill discovery and plan library cleanup"
  default_prompt: "Use $skill-library-manager to inspect my skill library and explain discovery or duplication problems."
policy:
  allow_implicit_invocation: true
